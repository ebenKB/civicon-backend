# npm 11.4.2's peer-dependency resolver crashes ("Cannot read properties of null
# (reading 'edgesOut')") while walking vitest 4.x's optional peer set. Skipping
# strict peer auto-install avoids that code path. Safe to remove once npm is
# upgraded past the fix.
legacy-peer-deps=true
